mutation createPost(
  $authorId: ID!
  $title: String!
  $content: String!
  $id: ID!
) {
  createPost(
    authorId: $authorId
    title: $title
    content: $content
    subforumId: $id
  ) {
    id
  }
}
